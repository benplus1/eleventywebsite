---
layout: "base"
eleventyExcludeFromCollections: true
title: "Portfolio"
---

<h1>Portfolio</h1>

<div class="posts">
  {% for post in collections.portfolio | reverse %}
    <a href="{{ post.url }}">
      <div class="post-summary">
        <div class="title">
          {{ post.data.title }}
        </div>
        <div class="date">{{ post.data.date | dateFormat }}</div>
        <div class="description">{{ post.templateContent | description }}</div>
        <div class="minutes">{{ post.templateContent | readingTime }} minute read</div>
      </div>
      <img src={{post.data.thumbnail}} class="img"></img>
    </a>
  {% endfor %}
</div>
